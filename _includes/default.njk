<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }} - Clear Code</title>
        <link rel="stylesheet" href="/assets/main.css">
        <meta property="og:image" content="{{shareImage}}">
        <meta property="og:title" content="{{title}}">
        <meta property="og:description" content="A book on writing clear, readable code">
        <meta name="twitter:image" content="{{shareImage}}">
        <meta name="twitter:title" content="{{title}}">
        <meta name="twitter:description" content="A book on writing clear, readable code">
        <meta name="twitter:card" content="summary_large_image">
        <meta name="twitter:creator" content="@CallMeWuz" />
    </head>
    <body>
        <div class="feedback">
            <a href="https://github.com/wuz/clear-code/issues/new?title=Feedback%20on%20%22{{title}}%22">Leave feedback</a>
        </div>
        <header>
            <div class="flex">
                <img src="/assets/logo.png" alt="Broom logo" class="logo"/>
                <a href="/">clear code</a>&nbsp;&mdash; a book by&nbsp;<a href="https://wuz.sh">wuz</a>
            </div>
            <div>
                {{ prevPage | safe }}&nbsp;{{ nextPage | safe }}
            </div>
        </header>

        <main class="content">
            <article>
                {{ content | safe }}
            </article>
        </main>
        <footer></footer>
    </body>
</html>
